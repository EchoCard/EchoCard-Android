# 复制为 local.properties 并填写。local.properties 已被 .gitignore。

# Release 签名（可选；不配置则 assembleRelease 为未配置 store 的 release 构建）
release.storeFile=key/release.keystore
release.storePassword=
release.keyAlias=callmate
release.keyPassword=

# Android SDK（由 Android Studio 自动生成）
# sdk.dir=/path/to/Android/sdk
