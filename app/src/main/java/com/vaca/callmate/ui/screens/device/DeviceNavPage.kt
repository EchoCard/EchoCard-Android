package com.vaca.callmate.ui.screens.device

/**
 * 设备管理全屏栈内子页（对标 iOS `DeviceModalView` NavigationStack + sheet → Android 全屏 push）。
 */
enum class DeviceNavPage {
    Main,
    Advanced,
    Diagnostics,
    Light
}
